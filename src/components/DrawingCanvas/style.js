import styled from "styled-components";

export const S = {
    MainWrapper: styled.div`
        canvas {
            position: absolute;
            top: 0;
            left: 0;
            user-select: none;
            z-index: 10000;
        }
    `,
};
